# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Persistent simulator state from `ocpp-cp-sim --state-db` /
# docker-compose. Ignored so a local dev run doesn't accidentally commit
# scenario history or queued-message rows.
.state/
*.db
*.db-wal
*.db-shm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri
src-tauri/target/
src-tauri/Cargo.lock
src-tauri/gen/

# Tauri sidecar — Bun-compiled CLI per target triple, produced at build
# time by scripts/build-tauri-sidecar.sh. Never committed.
src-tauri/binaries/
coverage/

# e2e Go CSMS fixture build artifact
e2e/csms/e2e-csms
